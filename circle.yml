version: 2

jobs:
  macos:
    macos:
      xcode: "10.2"
    steps:
      - checkout
      - run: swift build
      - run: swift test

  linux:
    docker:
      - image: codevapor/swift:5.0
    steps:
      - checkout
      - run: swift build
      - run: swift test
      - run: swift build -c release

workflows:
  version: 2
  tests:
    jobs:
      # - macos
      - linux
