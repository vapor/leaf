#define(type = file.mimeType.serialized)
#define(created = Date(timeStamp: file.creationDate,
                       fixedFormat: "YYYY-MM-dd hh:mm:ss"))
#define(modified = Date(timeStamp: file.contentModificationDate,
                        fixedFormat: "YYYY-MM-dd hh:mm:ss"))

#let(start = Timestamp())

Contents of #($index.requestPath) (#($index.absolutePath))
------------------------------------------------------------------
#for(file in $index.files):
#if(file.isRegularFile):
#(file.name) (#type()):  Created #created() / Modified #modified()

#endif
#endfor

Execution Time: #((Timestamp() - start) * 1_000) ms
